const headers = ["record_id", "first_name", "last_name", "preferred_name", "email", "roles", "other_role", "organization", "professional_title", "country_region", "general_field", "specific_research_area", "conference_updates_consent", "consent_timestamp", "contact_status", "duplicate_status", "submission_source", "admin_notes", "created_at", "updated_at", "archived_at"] as const;
function cell(value: unknown) { const text = Array.isArray(value) ? value.join("; ") : value === null || value === undefined ? "" : String(value); const safe = /^[=+\-@]/.test(text) ? `'${text}` : text; return `"${safe.replaceAll('"', '""').replace(/[\r\n]+/g, " ")}"`; }
export function contactsToCsv(rows: Record<string, unknown>[]) { const body = rows.map((row) => headers.map((header) => cell(header === "contact_status" ? row.status : row[header])).join(",")); return `\uFEFF${headers.join(",")}\r\n${body.join("\r\n")}\r\n`; }
