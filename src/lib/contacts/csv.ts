const columns = [
  ["Record ID", "record_id"], ["First Name", "first_name"], ["Last Name", "last_name"], ["Preferred Name", "preferred_name"], ["Email Address", "email"], ["Roles", "roles"], ["Other Role", "other_role"], ["Organization / Institution", "organization"], ["Position / Professional Title", "professional_title"], ["Country / Region", "country_region"], ["Broad Research / Professional Interest", "general_field"], ["Detailed Research / Professional Interest", "specific_research_area"], ["Permission to Receive Conference Updates", "conference_updates_consent"], ["Consent Timestamp", "consent_timestamp"], ["Contact Status", "status"], ["Duplicate Status", "duplicate_status"], ["Submission Source", "submission_source"], ["Admin Notes", "admin_notes"], ["Created At", "created_at"], ["Updated At", "updated_at"], ["Archived At", "archived_at"],
] as const;
function cell(value: unknown) { const text = (Array.isArray(value) ? value.join("; ") : value === null || value === undefined ? "" : String(value)).replace(/[\r\n]+/g, " "); const safe = /^\s*[=+\-@]/u.test(text) ? `'${text}` : text; return `"${safe.replaceAll('"', '""')}"`; }
export function contactsToCsv(rows: Record<string, unknown>[]) { const body = rows.map(row => columns.map(([, field]) => cell(row[field])).join(",")); return `\uFEFF${columns.map(([header]) => header).join(",")}\r\n${body.join("\r\n")}\r\n`; }
